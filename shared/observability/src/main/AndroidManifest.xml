<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application>
        <meta-data
            android:name="com.koflox.observability.collection_enabled"
            android:value="${observabilityCollectionEnabled}" />

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="com.koflox.observability.initializer.FirebaseInitializer"
                android:value="androidx.startup" />
            <meta-data
                android:name="com.koflox.observability.initializer.TimberInitializer"
                android:value="androidx.startup" />
        </provider>
    </application>

</manifest>
