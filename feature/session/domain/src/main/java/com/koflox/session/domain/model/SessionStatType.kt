package com.koflox.session.domain.model

enum class SessionStatType {
    TIME,
    DISTANCE,
    AVG_SPEED,
    TOP_SPEED,
    ALTITUDE_GAIN,
    ALTITUDE_LOSS,
    MOVING_TIME,
    IDLE_TIME,
    AVG_POWER,
    MAX_POWER,
    CALORIES_WEIGHT,
    CALORIES_POWER,
}
